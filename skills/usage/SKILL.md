---
name: usage
description: Check Gemini API model limits and quota usage for the active account.
---

# Usage Command

This skill displays the API rate limits (usage quota) for the currently active model and account.

When the user says `/usage`:
```bash
python3 ~/.gemini/config/plugins/agy-account-manager/scripts/account_manager.py usage
```

Always run the command and show the output directly to the user.
